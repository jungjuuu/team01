<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper-team01">

<!-- ID로 회원 정보 조회 -->
<select id="selectAccount" parameterType="String" resultType="com.edu.myboot.vo.AccountVO">
    SELECT * FROM accounts WHERE account_id = #{accountId}
</select>
	
<!-- <select id="findId" parameterType="com.edu.myboot.vo.AccountVO" resultType="com.edu.myboot.vo.memberVO">
    SELECT a.account_id
	FROM accounts a
	JOIN members m ON m.account_no = a.account_no
	WHERE m.member_name = #{memberName}
	AND m.member_mobile =#{memberMobile}
</select>
	 -->
</mapper>